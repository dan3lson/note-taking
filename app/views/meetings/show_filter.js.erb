var $ajax_container = $("#ajax-container");
var show_filter_partial = "<%= j render(partial: 'meetings/show_filter', locals: { meeting: @meeting, note_type: @note_type, index: @index }) %>";
$ajax_container.empty();
$ajax_container.hide().html(show_filter_partial).fadeIn(400);
